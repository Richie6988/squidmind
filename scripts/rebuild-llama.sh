#!/bin/bash
# rebuild-llama.sh — kept for reference, not needed for normal operation
# The prebuilt binary works fine for Qwen3, Llama3, Mistral, etc.
# Gemma4 is not supported by node-llama-cpp@3.18.1 regardless of build.
echo "Nothing to rebuild. node-llama-cpp prebuilt binary is used automatically."
echo "To support Gemma4, wait for node-llama-cpp to release a new version."
